# Тема: Принцип DRY (Don't Repeat Yourself) на рівні модуля
# ------------------------------------------------------------------------
# Логіку розрахунку площі винесено в окрему функцію в окремому модулі.
# Якщо формулу потрібно буде змінити (наприклад, урахувати одиниці
# вимірювання), достатньо змінити лише ЦЕ місце, а не шукати всі копії
# формули по всьому проєкту.


def calculate_area(length: float, width: float) -> float:
    return length * width
